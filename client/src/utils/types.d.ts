export type milliseconds = number;

export type Message = {
    id: number,
    title: string,
    subtitle: string,
    color: string,
};

export type Point = {
    x: number,
    y: number
};

export type Rect = {
    left: number,
    right: number,
    top: number,
    bottom: number,
    width: number,
    height: number
};
